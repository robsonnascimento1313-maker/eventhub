{
  "name": "eventhub-backend",
  "version": "1.0.0",
  "description": "Backend do EventHub com NestJS, Prisma e JWT",
  "main": "dist/main.js",
  "scripts": {
    "start": "nest start",
    "dev": "nest start --watch",
    "prisma:migrate": "npx prisma migrate dev",
    "prisma:generate": "npx prisma generate"
  },
  "dependencies": {
    "@nestjs/core": "^10.0.0",
    "@nestjs/common": "^10.0.0",
    "@nestjs/jwt": "^10.0.0",
    "prisma": "^5.0.0",
    "@prisma/client": "^5.0.0",
    "bcrypt": "^5.1.0",
    "dotenv": "^16.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "ts-node": "^10.0.0"
  }
}
